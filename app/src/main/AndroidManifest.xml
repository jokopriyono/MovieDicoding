<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="id.web.jokopriyono.moviedicoding">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission
        android:name="id.web.jokopriyono.moviedicoding.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="id.web.jokopriyono.moviedicoding.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <provider
            android:name=".data.database.DatabaseProvider"
            android:authorities="id.web.jokopriyono.moviedicoding"
            android:exported="true"
            android:readPermission="id.web.jokopriyono.moviedicoding.READ_DATABASE"
            android:writePermission="id.web.jokopriyono.moviedicoding.WRITE_DATABASE" />

        <activity android:name=".activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.detail.DetailMovieActivity" />

        <receiver android:name=".widget.MovieWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/movie_widget_info" />
        </receiver>
        <service
            android:name=".widget.StackWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <receiver
            android:name=".service.DailyReceiver"
            android:enabled="true" />
        <receiver
            android:name=".service.NowPlayReceiver"
            android:enabled="true" />
    </application>

</manifest>